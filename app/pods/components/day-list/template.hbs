<h2 class="text-2xl text-amber-400 p-1">
  Days
  <Ui::Counter @value={{this.totalDays}} />
</h2>

{{#if this.days.loading}}
  Loading...
{{else}}
  <ul>
    {{#each this.days.data.trackedDaysPaginated.edges as |day|}}
      <DayList::DayDisplay @day={{day.node}} />
    {{/each}}
  </ul>
{{/if}}